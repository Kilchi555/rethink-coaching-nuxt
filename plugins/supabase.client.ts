// plugins/supabase.client.ts
import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(async (nuxtApp) => {
  // Hier könntest du zusätzliche Logik für Supabase im Client hinzufügen, falls benötigt.
  // Das Nuxt Supabase Modul stellt normalerweise useSupabaseClient() direkt bereit,
  // sodass du hier möglicherweise gar nichts brauchst.
  // Wenn das Plugin leer ist, kannst du es auch einfach löschen,
  // es sei denn, du planst, dort Code zu platzieren.
})